CLICK OFERTAS PARAGUAI - ESTRUTURA PRINCIPAL

Este arquivo contém os componentes principais do sistema.
Para o código completo, acesse: codigo_completo_aplicativo.txt

=== PACKAGE.JSON ===
{
  "name": "click-ofertas-paraguai",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development tsx server/index.ts",
    "build": "vite build && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
    "start": "NODE_ENV=production node dist/index.js",
    "db:push": "drizzle-kit push"
  },
  "dependencies": {
    "react": "^18.3.1",
    "express": "^4.21.2",
    "drizzle-orm": "^0.39.1",
    "@neondatabase/serverless": "^0.10.4",
    "@tanstack/react-query": "^5.60.5",
    "wouter": "^3.3.5",
    "tailwindcss": "^3.4.17",
    "typescript": "5.6.3",
    "vite": "^5.4.19"
  }
}

=== PRINCIPAIS COMPONENTES ===

1. SERVER
- server/index.ts - Servidor Express principal
- server/routes.ts - Rotas da API
- server/storage.ts - Camada de dados
- server/db.ts - Conexão PostgreSQL

2. CLIENT  
- client/src/App.tsx - App principal React
- client/src/components/scratch-card.tsx - Componente raspadinha
- client/src/pages/public-flyer.tsx - Página pública
- client/src/pages/admin-dashboard.tsx - Dashboard admin

3. SHARED
- shared/schema.ts - Schema do banco + validações

=== FUNCIONALIDADES PRINCIPAIS ===

• Sistema de Raspadinhas (3 tipos)
• PWA com service worker
• Autenticação híbrida
• Sistema de moedas (Guarani + USD)
• Upload de imagens
• Analytics básico
• Export PNG/PDF

=== BANCO DE DADOS ===

Tabelas principais:
- users (usuários)
- stores (lojas) 
- products (produtos)
- promotions (promoções)
- coupons (cupons)
- sessions (sessões)

=== COMO USAR ===

1. npm install
2. Configure DATABASE_URL 
3. npm run db:push
4. npm run dev

=== DEPLOY ===

Pronto para Replit:
- Variáveis de ambiente configuradas
- PostgreSQL Neon integrado
- Build automático
- PWA funcional

Para o código completo com todos os detalhes,
consulte: codigo_completo_aplicativo.txt

Sistema funcional e pronto para produção! 🚀