# IA Vendedor Configuration
# Use LLM para naturalização de respostas (0 = desabilitado, 1 = habilitado)
USE_LLM_PARAPHRASE=0

# Tom das respostas do assistente
REPLY_TONE=vendedor_descontraido

# Fonte do catálogo de produtos
# Opções: "json", "http", "postgresql", "hybrid"
CATALOG_SOURCE=postgresql

# Para CATALOG_SOURCE=json
CATALOG_PATH=data/catalogo.sample.json

# Para CATALOG_SOURCE=http
# CATALOG_URL=https://api.exemplo.com/produtos

# Para CATALOG_SOURCE=hybrid (usa JSON como fallback)
CATALOG_FALLBACK_PATH=data/catalogo.sample.json

# Caminho do dicionário canônico
CANON_PATH=data/canon.json

# Token de acesso admin
ADMIN_TOKEN=devtoken

# APIs de LLM (para naturalização)
# OPENAI_API_KEY=sk-...
# GEMINI_API_KEY=AIz...